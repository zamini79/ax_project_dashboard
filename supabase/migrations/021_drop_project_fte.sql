-- ============================================
-- 021. 과제 '투입 인력(fte)' 제거
-- ============================================
-- 투입 인력 항목을 데이터·UI 전면 제거. projects.fte 컬럼 삭제.

ALTER TABLE projects DROP COLUMN IF EXISTS fte;
