-- ============================================
-- 028. 진행현황(헬스) 'none'(미진행, 표시 '-') 값 추가
-- ============================================
-- 아직 전혀 진행되지 않았다는 의미. 색상 없이 '-'로 표시(앱에서 처리).

ALTER TYPE project_health ADD VALUE IF NOT EXISTS 'none';
